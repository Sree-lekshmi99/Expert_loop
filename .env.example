# Optional. Demo mode does not need credentials and makes no external API calls.
OPENAI_API_KEY=
# Use a model available to your account that supports Responses structured outputs.
OPENAI_MODEL=
EXPERTLOOP_DATA_DIR=./state
EXPERTLOOP_PORT=8000
